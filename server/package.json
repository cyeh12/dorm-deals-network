{
  "name": "college-student-marketplace-server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "build": "echo \"no-op\"",
    "heroku-postbuild": "cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "pg": "^8.11.5"
  },
  "engines": {
    "node": "20.x"
  }
}
